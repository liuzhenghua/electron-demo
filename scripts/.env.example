# 镜像名称与标签
FRONTEND_IMAGE=electron-demo-frontend
SERVER_IMAGE=electron-demo-server
IMAGE_TAG=latest

# 依赖安装源，可在私有网络中改为内部 npm registry。
NPM_REGISTRY=https://registry.npmmirror.com

# 可选。留空时，Web 使用当前页面主机的 4123 端口，Electron 使用本机 4123 端口。
# Vite 构建变量会公开在前端产物中，禁止填写密钥。
VITE_SERVER_URL=

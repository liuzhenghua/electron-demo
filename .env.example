# 可选：配置后 Electron 加载此 UI 地址；留空或删除则加载打包进应用的 dist/index.html。
# 开发时修改端口，Vite 会自动使用相同配置。
ELECTRON_UI_URL=http://localhost:5173

# 可选：SDK 私有下载源，支持 http(s):// 和 file://。地址必须指向包含 manifest.json 的目录。
# RUNTIME_DOWNLOAD_BASE_URL=file:///绝对路径/electron-demo/runtime-packages/
# 本地开发时设为 true，可强制走与安装包相同的下载、安装流程。
# RUNTIME_INSTALL_IN_DEVELOPMENT=true
